"""
Unit tests for src/ingest.py.

To be filled in once load_documents and split_into_chunks are implemented.
Example cases to cover:
    - load_documents raises a clear error on an empty folder
    - split_into_chunks preserves source document name and page number in metadata
    - chunk overlap actually overlaps (shared text between consecutive chunks)
"""
